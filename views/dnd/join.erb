<h1>Super Awesome DND Party Join Form</h1>
<p class="padded-bottom">So you'd like to join our party, that's great! Just so you know, we can't
accept anyone lower than level 8 (you'd be too far behind!) and also no one over
level 10.</p>
<form accept-charset="UTF-8" action="/dnd/join" method="post" class="form-horizontal">
  <div class="form-group">
    <label class="col-sm-2 control-label" for="name">Your Name: </label>
    <div class="col-sm-10">
      <input name="name" type="text" placeholder="<%= locals[:new_member][:name] %>">
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-2 control-label" for="class">Your Class: </label>
    <div class="col-sm-10">
      <input name="class" type="text" placeholder="<%= locals[:new_member][:class] %>">
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-2 control-label" for="level">What level are you on? </label>
    <div class="col-sm-10">
      <input name="level" type="text" placeholder="<%= locals[:new_member][:level] %>">
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-2">
      <button type="submit" class="btn btn-default">Join!</button>
    </div>
    <% if locals[:error] %>
      <div class="col-sm-4 error-text"><%= locals[:error] %></div>
    <% end %>
  </div>
</form>